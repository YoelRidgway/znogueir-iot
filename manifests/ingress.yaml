apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: playground-app
  namespace: dev
spec:
  rules:
  - host: localhost  # Works fine!
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: playground-app
            port:
              number: 8888
